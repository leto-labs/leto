# Tasks: add-opencode-compat-surface

## Archival Cleanup

- [x] Confirm legacy `brain-server` never implemented an OpenCode compatibility
      layer
- [x] Confirm current OpenCode compatibility work belongs to `agent-server`
      rather than to a future `brain-server` plan
- [x] Re-scope this change as superseded rather than planned implementation
      work
- [x] Preserve the proposal/design only as historical context for the
      abandoned legacy-scoped planning direction
- [x] Validate the change with
      `openspec validate add-opencode-compat-surface --strict`
- [x] Archive the change without promoting its legacy-scoped deltas into
      canonical specs
