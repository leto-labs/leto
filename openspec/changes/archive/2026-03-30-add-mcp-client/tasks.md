# Tasks: add-mcp-client

## Archival Cleanup

- [x] Confirm the proposal only targets archived legacy `brain-tools`
- [x] Re-scope this change as superseded rather than implementation work
- [x] Preserve the proposal/spec only as historical context for the abandoned
      legacy-scoped MCP direction
- [x] Validate the change with `openspec validate add-mcp-client --strict`
- [x] Archive the change without promoting its `brain-tools` deltas into
      canonical specs
