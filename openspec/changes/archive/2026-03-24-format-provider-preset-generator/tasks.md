## 1. Generator formatting

- [x] Format generated preset files inside `provider-preset-gen`
- [x] Make `--check` validate formatted output

## 2. Generated output

- [x] Regenerate `provider-openai` preset files

## 3. Validation

- [x] Run `cargo run -p provider-preset-gen -- --check`
- [x] Run relevant workspace tests
- [x] Validate and archive the OpenSpec change
