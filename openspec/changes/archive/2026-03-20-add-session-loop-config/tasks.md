# Tasks: add-session-loop-config

- [x] Add OpenSpec deltas for `brain-acp`, `brain-core`, and `brain-types`
- [x] Add `set_session_loop()` to the shared runtime surface
- [x] Add runtime tests for valid and invalid session loop updates
- [x] Extend real `brain-acp` config options with `loop`
- [x] Omit real `loop` config when only one loop is registered
- [x] Extend mock ACP session config with multi-value `loop`
- [x] Add backend and mock ACP tests for loop config updates
- [x] Run `cargo test --workspace`
- [x] Validate and archive the OpenSpec change
