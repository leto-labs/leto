- [x] Add `provider` requirements for the shared credential module, pool, and
      built-in strategies.
- [x] Add `provider-openai` requirements for the standalone OAuth surface and
      pool-backed provider adapters.
- [x] Add `agent-store` requirements for persisted OAuth metadata and
      credential health state.
- [x] Add `agent-core` requirements for activating stored credentials into the
      shared provider pool and persisting pool health back to the store.
- [x] Validate the focused change with `openspec validate
      add-provider-credential-pool-and-openai-oauth --strict`.
- [x] Archive the focused change so canonical specs are updated immediately.
