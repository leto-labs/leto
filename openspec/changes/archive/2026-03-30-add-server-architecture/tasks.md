# Tasks: add-server-architecture

## Archival Cleanup

- [x] Confirm the current remote/server direction is `agent-server` plus
      `agent-core-remote`, not future `brain-server` work
- [x] Confirm this legacy-centered architecture plan should not remain active
      for migration cleanup
- [x] Re-scope this change as superseded rather than deferred implementation
      work
- [x] Preserve the proposal/design only as historical context for the
      abandoned `BrainRuntimeRemote` planning frame
- [x] Validate the change with `openspec validate add-server-architecture
      --strict`
- [x] Archive the change without promoting its legacy-centered deltas into
      canonical specs
