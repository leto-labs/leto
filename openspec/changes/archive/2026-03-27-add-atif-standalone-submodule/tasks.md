- [x] Add the `submodules/atif-rust` SSH submodule entry.
- [x] Bootstrap `submodules/atif-rust` as a standalone Rust workspace with MIT
  licensing, CI, and public crate metadata.
- [x] Port the current `crates/atif` code and tests into the standalone repo.
- [x] Update the standalone README for external users and document the Harbor
  compatibility workflow.
- [x] Validate the OpenSpec change.
